using Verse;

namespace BPCSynchronizer
{
    public class BpcSynchronizer : Mod
    {
        public BpcSynchronizer(ModContentPack content) : base(content)
        {
            Log.Message("BPC Synchronizer loaded!");
        }
    }
}
